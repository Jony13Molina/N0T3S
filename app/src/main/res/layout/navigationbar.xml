<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:id="@+id/nav_header"
    android:layout_width="match_parent"
    android:layout_height="160dp"
    android:background="@color/softeRealBlue"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/AppBaseTheme">


    <ImageView
        android:id="@+id/nav_header_imageView"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@mipmap/twotone_assignment_ind_black_48" />

    <TextView
        android:id="@+id/nav_header_textView"
        android:layout_width="255dp"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:layout_marginTop="20dp"
        android:text="Hello"
        android:textColor="@color/nightBlack"
        android:textSize="14sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:orientation="horizontal">


        <TextView
            android:id="@+id/nav_header_numRate"
            android:layout_width="31dp"
            android:layout_height="34dp"
            android:layout_gravity="right"
            android:layout_marginTop="20dp"
            android:text="5.0"
            android:textColor="@color/nightBlack"
            android:textSize="14sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/nav_header_star"
            android:layout_width="20dp"
            android:layout_height="21dp"
            android:layout_gravity="right"
            android:src="@mipmap/ic_rateicon" />


    </LinearLayout>


</LinearLayout>
